version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No se requieren dependencias para el frontend estático"
    build:
      commands:
        - echo "Frontend estático - no se requiere build"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
backend:
  phases:
    build:
      commands:
        - echo "Backend build phase"
        - pip install -r requirements.txt
  artifacts:
    baseDirectory: api
    files:
      - '**/*'

